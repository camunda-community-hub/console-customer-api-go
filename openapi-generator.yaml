inputSpec: openapi.json
output: ./

generatorName: go

gitUserId: sijoma
gitRepoId: console-customer-api-go

validateSpec: false

modelNameMappings:
  # `ClusterStatus` conflicts with the autogenerated Cluster's `status`
  ClusterStatus: ClusterComponentStatus

# Prevent similar names to clash
enumClassPrefix: true
